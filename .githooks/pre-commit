#!/usr/bin/env bash
set -euo pipefail
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
"$repo_root/scripts/update-version.sh"
# Ensure the updated header is part of the commit
cd "$repo_root"
git add components/header.html
